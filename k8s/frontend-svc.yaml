apiVersion: v1
kind: Service
metadata:
  name: frontend-lb
  namespace: capstone-project
spec:
  type: LoadBalancer
  selector:
    app: app
    version: blue
  ports:
  - port: 80
    targetPort: 3000
